import React, { useEffect, useState } from "react";
import {} from "react-router-dom";

function Home() {
	let [data, setData] = useState([]);

	useEffect(() => {}, []);

	return (
		<>
			<h1>Home page you got it !!</h1>
		</>
	);
}

export default Home;
